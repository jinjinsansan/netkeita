// Single source of truth for the public site URL. Used by canonical links,
// OGP metadata, and share buttons so we don't scatter hard-coded origins
// across components.
export const SITE_URL = "https://www.netkeita.com";
